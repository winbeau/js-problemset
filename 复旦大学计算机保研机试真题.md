[**完整题目及在线评测：https://pgcode.cn/**](https://pgcode.cn/)

## 拼出fudan-复旦大学

#### 题目描述

给定一个字符串 ${s}$，只包含英文字母（大小写均有）。你可以使用字符串中的字母来拼出单词 "fudan"（不区分大小写，即可以使用 'F' 或 'f' 表示 'f'，以此类推）。每个字母在字符串中只能使用一次。问：最多能拼出多少个完整的 "fudan"？

#### 输入格式

- 一行字符串 ${s}$（长度范围：${1 \le |s| \le 1000}$），仅包含大小写英文字母。

#### 输出格式

- 一个整数，表示最多能拼出多少个完整的 "fudan"。

#### 数据范围

- ${1 \le |s| \le 1000}$

#### 输入样例1

```
FfuUddaANNn
```

#### 输出样例1

```
2
```



## 非日型皇后放置-复旦大学

待添加

## 可达方格数查询-复旦大学

待添加

## INTa语言解释器-复旦大学

#### 题目描述

实现一个简单的编程语言解释器，用于执行INTa语言程序。该语言支持以下语句：

1. INTa：定义一个变量a（变量名为单个字母），初始值为0

2. a=exp：为变量a赋值，其中exp是表达式

3. PRINTexp：输出表达式exp的计算结果

4. IF exp...ENDIF：条件语句，如果exp的值非0，则执行IF和ENDIF之间的语句

表达式计算规则：

- 从左到右计算，不考虑运算符优先级

  - 例如：${2+3*4}$ 计算为 ${(2+3)*4 = 20}$，而不是 ${2+(3*4) = 14}$

  - 例如：${5-3*2}$ 计算为 ${(5-3)*2 = 4}$，而不是 ${5-(3*2) = -1}$

- 支持的操作符：

  - ${+}$：加法

  - ${-}$：减法

  - ${*}$：乘法

  - ${/}$：整数除法（向0取整）

  - ${\&}$：按位与

  - `^`：按位异或

- 操作数类型：

  - 立即数：0-999之间的整数

  - 变量：单个字母（a-z）

- 除法规则：整数除法，向0取整

  - 例如：${7/3 = 2}$，${-7/3 = -2}$

- 按位运算：按照标准规则执行

作用域规则：

- 每个IF语句创建一个新的作用域

- 允许在不同作用域内重新定义变量：

  - 在IF内部可以重新定义变量，这些变量在ENDIF后不再存在

  - 外部作用域的变量在内部作用域可见，但可能被覆盖

- 变量查找规则：

  - 从内层到外层依次查找变量

  - 赋值时，修改最近作用域中已存在的变量，或在当前作用域定义新变量

程序执行规则：

- 语句按顺序执行

- IF语句根据条件决定是否执行其内部语句

- 变量在定义前不可使用（假设输入总是合法的）

#### 输入格式

- 多行字符串，表示INTa语言的程序

- 每行一个语句

- 语句类型包括：

  - INTa：定义变量（a为单个字母）

  - a=exp：赋值语句（a为单个字母）

  - PRINTexp：打印语句

  - IF exp：条件开始

  - ENDIF：条件结束

#### 输出格式

- 对于每个PRINT语句，输出表达式计算结果

- 每个结果占一行

#### 数据范围

- 输入程序不超过100行

- 变量名均为单个小写字母（a-z）

- 立即数范围为0-999

#### 输入样例1

```
INTa
a=5
INTb
b=3
PRINTa+b
PRINTa-b
PRINTa*b
PRINTa/b
PRINTa&b
PRINTa^b
```

#### 输出样例1

```
8
2
15
1
1
6
```



## 最长不下降子序列-复旦大学

待添加

## 小明学习时间-复旦大学

待添加

## 皇后大战-复旦大学

#### 题目描述

在一个无限大的棋盘上，安放了若干皇后。每个皇后可以攻击到其所在位置的行、列以及两条对角线上的其他皇后。现在需要将这些皇后划分为两个阵营A阵营和B阵营，满足以下条件：

1. 同一阵营内的皇后不能互相攻击（即同一阵营内任意两个皇后不能在同一行、同一列或同一条对角线上）

2. 不同阵营的皇后可以互相攻击（无限制）

3. 每个阵营至少包含一个皇后

给定所有皇后的坐标，计算满足上述条件的阵营划分方案总数。

#### 输入格式

- 第一行：一个整数 $n$（$1 \leq n \leq 10^5$），表示皇后的数量

- 接下来 $n$ 行：每行两个整数 $x_i$ 和 $y_i$（$-10^9 \leq x_i, y_i \leq 10^9$），表示第 $i$ 个皇后的坐标

  注意：保证所有皇后的坐标互不相同

#### 输出格式

- 一个整数，表示满足条件的阵营划分方案总数

- 由于结果可能很大，请输出对 $10^9+7$ 取模后的结果

#### 数据范围

$1 \leq n \leq 10^5$，坐标范围 $-10^9 \leq x_i, y_i \leq 10^9$

#### 输入样例1

```
3
0 0
0 1
1 0
```

#### 输出样例1

```
2
```



## 括号字符串处理-复旦大学

待添加

## 双向TSP/Bitonic TSP-复旦大学

待添加

## 特殊的最短路-复旦大学

#### 题目描述

给定一个有向图，节点编号为非负整数，图中每条边有一个正整数长度。对于从起点 $s$ 到节点 $t$ 的任意一条简单路径（路径中不重复经过节点），其“特殊距离”定义为：

特殊距离 = (路径上所有边的长度总和) + (路径上的最短边长度) - (路径上的最长边长度)

请计算起点 $s$ 到图中所有其他节点的最短特殊距离。若节点不可达，输出 -1。

#### 输入格式

第一行包含两个整数 $N$ 和 $M$ ($1 \leq N < 10^5$, $1 \leq M \leq 3 \times 10^5$)，分别表示节点数和边数。

接下来 $M$ 行，每行包含三个整数 $u, v, w$，表示一条从 $u$ 到 $v$ 的有向边，长度为 $w$ ($1 \leq w \leq 10^4$)。

最后一行包含一个整数 $s$，表示起点 ($0 \leq s < N$)。

#### 输出格式

共 $N$ 行，第 $i$ 行表示起点 $s$ 到节点 $i$ 的最短特殊距离。若不可达，输出 -1。

#### 数据范围

- $1 \leq N < 10^5$

- $1 \leq M \leq 3 \times 10^5$

- $1 \leq w \leq 10^4$

- $0 \leq s < N$

#### 输入样例1

```
3 3  
0 1 5  
0 2 10
1 2 3
0
```

#### 输出样例1

```
0
5
6
```



## 字符串的编辑距离-复旦大学

待添加

## 二叉树前中后序遍历-复旦大学

待添加

## 字符串的重复输出-复旦大学

#### 题目描述

给一个字符串比如 $ABC$ 再给一个整数比如 $3$ 输出 $AAABBBCCC$ 就行了。

#### 输入样例
```
ABC 3
```
#### 输出样例
```
AAABBBCCC
```

## 最小权值序列-复旦大学

待添加

## 二叉搜索树-复旦大学

待添加

## Hanoi 塔问题-复旦大学

#### 题目描述

Hanoi 塔问题是印度的一个古老的传说。

开天辟地的神勃拉玛在一个庙里留下了三根金刚石的棒，第一根上面套着 $64$ 个圆的金片，最大的一个在底下，其余一个比一个小，依次叠上去。

庙里的众僧不倦地把它们一个个地从这根棒搬到另一根棒上，规定可利用中间的一根棒作为帮助，但每次只能搬一个，而且大的不能放在小的上面。

请编写程序，把 $A$ 柱上的 $n$ 个金片，搬动到 $C$ 柱（中间可以使用 $B$ 柱），使得搬动的次数最少。

输入金片的个数 $n$（$1 <= n <= 64$），输出总搬动次数，以及最后 $100$ 次搬动。

如果搬动次数小于等于 $100$ 则全部输出；每个搬动占一行，加上是这第几次搬动的数字和“:”，格式见示例。

#### 输入格式

输入一个整数 $n$，表示金片的个数（$1 <= n <= 64$）。

#### 输出格式

第一行输出一个整数，表示总搬动次数。

接下来的每一行输出一个搬动步骤，格式为“次数:起始柱->目标柱”。

如果搬动次数大于 $100$，则只输出最后 $100$ 次搬动。

#### 输入样例

```
2
```

#### 输出样例

```
3
1:A->B
2:A->C
3:B->C
```

## 跳台阶问题-复旦大学

待添加

## 最大连续子序列-复旦大学

待添加

## 双向 TSP-复旦大学

#### 题目描述

对原始 $TSP$ 问题加上限制条件，地点的坐标为二维的，旅行商只能从左到右走，然后再从右到左回到起点，求最短的路径。

#### 输入格式

地点地址（地点横坐标互不相同，地点数量少于 $1000$）

#### 输出格式

最短路径

#### 输入样例

```
1 2
3 4
5 6
```

#### 输出样例

```
11.314
```

## 长方形中的正方形-复旦大学

待添加

## 众数问题-复旦大学

待添加

## 骨牌-复旦大学

#### 题目描述

有 $2 \times n$ 的地板，用 $1 \times 2$ 和 $2 \times 1$ 的骨牌进行铺地板。

问共有多少种情况。

结果对 $999983$ 取余，$1 \leq n \leq 10^{12}$。

#### 输入格式

输入一个整数 $n$。

#### 输出格式

输出一个整数，表示有多少种情况。

#### 输入样例

```
6
```

#### 输出样例

```
13
```

## 特殊的最短路-复旦大学

待添加

## 打地鼠-复旦大学

待添加

## 取负数或原值的方案数-复旦大学

#### 题目描述

给定一个非负整数序列 $x_1, x_2, \dots, x_n$，可以给每一个整数取负数或者取原值。

求有多少种取法使得这些整数的和等于期望值 $E$。

结果对 $10^9 + 7$ 取模。

#### 输入格式

第一行包含两个整数 $N$ 和 $E$，分别表示序列长度和期望值。

第二行包含 $N$ 个非负整数，表示序列 $x_1, x_2, \dots, x_n$。

#### 输出格式

输出一个整数，表示满足条件的方案数，结果对 $10^9 + 7$ 取模。

#### 输入样例

```
5 3
1 1 1 1 1
```

#### 输出样例

```
5
```

## 排队打饭-复旦大学

待添加

## A 与 B 得到 C-复旦大学

待添加

## 解一元一次方程-复旦大学

#### 题目描述

解方程，给定一个字符串，代表一个一元一次方程。

如果有解求解，输出格式“$x=$数字”，如果解的个数无穷，输出“infinite solutions”。

如果没有解输出“no solution”，字符串长度不超过 $256$。

#### 输入格式

给定一个字符串，代表一个一元一次方程。

#### 输出格式

如果有解，输出“$x=$数字”；如果解的个数无穷，输出“infinite solutions”；如果没有解，输出“no solution”。

#### 输入样例

```
2x+4-3x=x-2
```

#### 输出样例

```
x=3
```

## 序列权值的最小值-复旦大学

待添加

## 求最大公共子串长度-复旦大学

待添加

## 最大公共子串-复旦大学

#### 题目描述

输入 $3$ 个子串，输出这 $3$ 个子串的最大公共子序列。

#### 输入格式

输入包含 $3$ 个字符串串，用空格分隔。

#### 输出格式

输出这 $3$ 个子串的最大公共子序列。

#### 输入样例

```
abcd acb abc
```

#### 输出样例

```
ab
```

## 字符串的编辑距离-复旦大学

待添加

## 长方形中的正方形-复旦大学

待添加

## 中位数-复旦大学

#### 题目描述

给定一个整数序列，求中位数。

如果序列个数为奇数，中位数为升序排列的中间位置的数；如果是偶数，则为升序排列的中间两个数的平均值。

#### 输入格式

输入包含多组测试数据，每一组第一行为一个整数 $n$ ($n < 10^4$) 表示这个序列的个数，接下来有 $n$ 个整数 $k$ ($0 < k < 2^{31} - 1$)。

#### 输出格式

输出这个序列的中位数。

#### 输入样例

```
5
2 1 4 3 5
```

#### 输出样例

```
3
```

## 后缀序列求值-复旦大学

待添加

## 最长公共连续子串-复旦大学

待添加

## 日期天数计算-复旦大学

#### 题目描述

输入一个日期，格式为 $YYYYMMDD$，求该日期与 $20190205$ 相隔的天数。

#### 输入格式

输入一个日期，格式为 $YYYYMMDD$。

#### 输出格式

输出该日期与 $20190205$ 相隔的天数。

#### 输入样例

```
20190208
```

#### 输出样例

```
3
```

## 字符串编辑距离-复旦大学

待添加

## 哈夫曼编码的最短长度-复旦大学

待添加

## 二叉树最大叶子间距-复旦大学

#### 题目描述

给定一棵二叉树，每个节点有一个唯一的编号。

给出每个节点的左右子树的编号（用 $-1$ 表示空），求这棵二叉树中两个叶子节点之间的最大间距。

#### 输入格式

第一行包含一个整数 $N$，表示二叉树的节点数量。

接下来的 $N$ 行，每行包含两个整数，表示对应节点的左子树编号和右子树编号（用 $-1$ 表示空）。

#### 输出格式

输出一个整数，表示二叉树中两个叶子节点之间的最大间距。

#### 输入样例 1

```
3
1 2
-1 -1
-1 -1
```

#### 输出样例 1

```
2
```

#### 输入样例 2

```
6
1 -1
2 3
4 -1
-1 5
-1 -1
-1 -1
```

#### 输出样例 2

```
4
```

## 铺地板问题-复旦大学

待添加

## 二叉树的形态数-复旦大学

待添加

## 素数障碍最短路径-复旦大学

#### 题目描述

给定一个表格，求两个位置之间的最短距离。

其中素数位置不能经过。

**此题不完整，仅供参考，无法作答。**

#### 输入格式

输入包含多组测试用例。

每组测试用例包含一个整数，表示目标位置。

#### 输出格式

对于每组测试用例，输出最短距离。

如果无法到达，输出 $impossible$。

#### 输入样例

```
3
14
9321012
```

#### 输出样例

```
Case 1: 1
Case 2: 7
Case 3: impossible
```

## 中序与后序求层次遍历-复旦大学

待添加

## 斗牛-复旦大学

待添加

## 计算学习时间-复旦大学

#### 题目描述

小明每周的每一天都会去实验室，他严格设置了一周中每一天到达与离开实验室的时间，且一个学期中的每一周都遵循这样的时间安排。

一天中的午饭和晚饭时间是固定的，分别是 $12:00$ - $13:00$ 和 $17:00$ - $18:00$，在午饭和晚饭时间中小明是不学习的，其他时间内小明都在实验室内学习。

请你计算一个学期内小明的总学习时间（分钟）。

#### 输入格式

第一行：学期数。

第二行：一个学期的天数，该学期第一天是星期几。

第三行到第九行：周一到周日小明到达实验室与离开实验室的时间（$小时:分钟$）。

剩下为不同学期的时间信息，格式与前几行一样。

#### 输出格式

总时间（分钟）。

#### 输入样例

```
1
7 1
08:00 12:00
08:00 12:00
08:00 12:00
08:00 12:00
08:00 12:00
08:00 12:00
08:00 12:00
```

#### 输出样例

```
1680
```

## 字符串匹配-复旦大学

待添加

## A Famous ICPC Team-复旦大学

待添加

## 二分查找次数计算-复旦大学

#### 题目描述

大家一定都能熟练掌握二分查找啦！那么来计算二分的次数吧！约定二分的

中点 $mid = (left + right) / 2$。

#### 输入格式

第一行输入一个整数 $N$ ($N \leq 10000$)。

第二行输入 $N$ 个升序整数。

第三行输入一个待查找的整数（必定在第二行中出现过）。

#### 输出格式

输出二分查找该整数时，进行过多少次二分。

#### 输入样例

```
5
1 8 53 54 99
53
```

#### 输出样例

```
1
```

## 判断两数能否通过运算得到第三数-复旦大学

待添加

## 成绩排序-复旦大学

待添加

## 最大连续子序列和-复旦大学

#### 题目描述

给定一个数组及其大小，数组元素在 $[-10, 10]$，元素个数最多 $10^6$ 个。

输出最大的连续字串和，题目保证最后结果为正数。

#### 输入格式

输入包含一个数组及其大小。

#### 输出格式

输出最大的连续子串的和。

#### 输入样例

```
5
1 -2 3 5 -1
```

#### 输出样例

```
8
```

## 字符串括号处理-复旦大学

待添加

## ISBN 校验位计算-复旦大学

待添加

## 优先队列操作实现-复旦大学

#### 题目描述

给出优先队列的实现，实现 4 个操作：

- $ADD(N, P)$: 往队列里加入 $id$ 为 $N$ 的优先级为 $P$ 的任务

- $NEXT$: 输出下一个最高优先级的任务的 $id$，如果优先级相同输出 $id$ 小的任务，若队列中没有任务输出 $-1$

- $REMOVE(N)$: 移除 $id$ 为 $N$ 的任务

- $COUNT$: 输出队列中的任务数量

#### 输入格式

第一行输入操做数。

接下来输入操做行，每行表示一个操作。

操作格式如下：

- $ADD\ N\ P$

- $NEXT$

- $REMOVE\ N$

- $COUNT$

#### 输出格式

对于每个 $NEXT$ 和 $COUNT$ 操作，输出相应的结果。

#### 输入样例

```
10
ADD 1 1
ADD 2 3
ADD 3 2
ADD 4 3
NEXT 
REMOVE 3
COUNT 
NEXT
NEXT
NEXT
```

#### 输出样例

```
2
2
4
1
-1
```

