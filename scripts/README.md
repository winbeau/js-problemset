# Scripts

- `restructure_problemset.py`：将根目录学校级题库归档到 `raw/`，切分生成 `schools/`，并产出 `docs/roadmap/`。
- `package_nowcoder_week.py`：将 `nowcoder/<week>/` 打包为单题 zip 与整周 zip，默认输出到 `nowcoder/packages/<week>/`。
