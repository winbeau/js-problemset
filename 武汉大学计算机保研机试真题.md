[**完整题目及在线评测：https://pgcode.cn/**](https://pgcode.cn/)

## 区间内最近的质数对-武汉大学

#### 题目描述

给定两个正整数 $L$ 和 $R$（$1 \leq L \leq R \leq 10^6$），请找出区间 $[L, R]$ 中距离最近的两个不同质数（即两个质数的差的绝对值最小）。若存在多对距离相同的质数对，选择数值较小的那对；若区间内质数数量少于 $2$ 个，输出 `-1 -1`。

#### 输入格式

一行两个整数 $L$ 和 $R$

#### 输出格式

一行两个整数，表示距离最近的质数对（从小到大）；若无则输出 `-1 -1`

#### 数据范围

$1 \leq L \leq R \leq 10^6$

#### 输入样例

```
10 20
```

#### 输出样例

```
11 13
```



## 柱状图中最大的矩形-武汉大学

待添加

## 夜间过桥问题-武汉大学

待添加

## 矩阵行调整-武汉大学

#### 题目描述

输入一个 $N$ ($N \leq 10$) 阶方阵，按照如下方式调整方阵：

1. 将第一列中最大数所在的行与第一行对调。

2. 将第二列中从第二行到第 $N$ 行最大数所在的行与第二行对调。

3. 依此类推，直到第 $N-1$ 列中从第 $N-1$ 行到第 $N$ 行最大数所在的行与第 $N-1$ 行对调。

4. 输出调整后的方阵。

#### 输入格式

包含多组测试数据，每组测试数据第一行为一个整数 $N$，表示方阵的阶数。

接下来输入这个 $N$ 阶方阵。

#### 输出格式

调整后的方阵。

#### 输入样例
```
4
3687
6753
8653
9872
```

#### 输出样例
```
9872
6753
3687
8653
```

## 数字字符串相加-武汉大学

待添加

## 回文质数-武汉大学

待添加

## 数字金字塔-武汉大学

#### 题目描述

输入一个整数 $n$ $(0 < n < 10)$，显示 $n$ 行如下规律图形。

#### 输入格式

一个整数 $n$。

#### 输出格式

输出 $n$ 行数字金字塔。

#### 输入样例
```
3
```

#### 输出样例
```
1
23
456
78910
```

## 数字统计-武汉大学

待添加

## 猴子吃桃问题-武汉大学

待添加

## 寻找比给定数大的最小不重复数-武汉大学

#### 题目描述

给定一个数 $n$，若该数的相邻数字都不相同，则称该数为不重复数（例如 $110$ 为重复数，$101$ 为不重复数）。

编程实现比 $n$ 大的最小不重复数。

#### 输入格式

输入一个整数 $n$。

#### 输出格式

输出比 $n$ 大的最小不重复数。

#### 输入样例
```
110
```

#### 输出样例
```
120
```

## 统计数字出现次数和位置-武汉大学

待添加

## 兔子繁殖问题-武汉大学

待添加

## 质因数分解-武汉大学

#### 题目描述

将一个正整数分解质因数。

例如：输入 $90$，打印出 $90 = 2 \times 3 \times 3 \times 5$。

#### 输入格式

输入一个正整数 $n$。

#### 输出格式

输出 $n$ 的质因数分解结果，格式为 $n = a \times b \times c \times \dots$。质因子从小到大输出，各个符号之间有一个空格，乘号用 `*` 代替。

#### 输入样例
```
90
```

#### 输出样例
```
90 = 2 * 3 * 3 * 5
```

## 倒杨辉三角-武汉大学

待添加

## 苹果分配问题-武汉大学

待添加

## 质因数分解-武汉大学

#### 题目描述

任何一个合数都可以写成几个 $质数$ 相乘的形式。

请编写程序分解 $质因数$。

#### 输入格式

输入一个合数 $n$。

#### 输出格式

输出 $n$ 的 $质因数$ 分解形式，格式为 $n = p_1 \times p_2 \times \ldots \times p_k$，其中 $p_i$ 为 $质数$，且 $p_1 \leq p_2 \leq \ldots \leq p_k$。

#### 输入样例
```
12
```

#### 输出样例
```
12 = 2 × 2 × 3
```

## 数字金字塔图案-武汉大学

待添加

## 打印斐波那契数列中的质数-武汉大学

待添加

## 球组合问题-武汉大学

#### 题目描述

袋子里有 $2$ 个红球， $3$ 个绿球， $5$ 个黄球。

随机从中摸出 $8$ 个球，打印显示所有可能的组合。

#### 输入格式

无输入。

#### 输出格式

输出所有可能的球组合，每行一个组合。

组合格式为：红球数量、绿球数量、黄球数量，用空格分隔。

#### 输入样例
```
无
```

#### 输出样例
```
0 3 5
1 2 5
1 3 4
2 1 5
2 2 4
2 3 3
```

## 数字分解显示-武汉大学

待添加

## IP地址十进制转二进制-武汉大学

待添加

## 正整数分解质因数-武汉大学

#### 题目描述

将一个正整数分解质因数。

例如：输入 $90$，打印出 $90 = 2 * 3 * 3 * 5$。

#### 输入格式

多组输入数据，每组数据为一个正整数。

#### 输出格式

输出该正整数的质因数分解结果，格式如 $2 * 3 * 3 * 5$。

如果输入数字本身是一个质数，则不输出任何结果。

#### 输入样例
```
10
90
```

#### 输出样例
```
2 * 5
2 * 3 * 3 * 5
```

## 找到整数位置-武汉大学

待添加

## 调整方阵-武汉大学

待添加

## 不重复数-武汉大学

#### 题目描述

给定一个数，若该数的相邻数字都不相同，则称该数为不重复数（如 $110$ 为重复数，$101$ 为不重复数）。

输入 $n$，编程实现比 $n$ 大的最小不重复数。

#### 输入格式

多组数据输入。

#### 输出格式

输出比 $n$ 大的最小不重复数。

#### 输入样例
```
5
10
20
```

#### 输出样例
```
6
12
21
```

## 简单模式匹配-武汉大学

待添加

