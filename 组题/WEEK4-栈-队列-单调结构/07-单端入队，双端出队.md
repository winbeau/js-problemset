# 单端入队，双端出队

## 题目描述

考虑一种特殊的队列：它只允许在一端插入元素，但允许从两端删除元素。

给定一个入队序列，你需要判断若干个出队序列是否有可能通过这种结构得到。

例如，若按顺序插入 $1, 2, 3, 4, 5$，则可以得到输出序列 $1, 3, 2, 5, 4$，但不可能得到 $5, 1, 3, 2, 4$。

## 输入描述

每个输入文件包含一组测试数据。

第一行给出两个正整数 $N$ 和 $K$（$\leq 10$），分别表示入队元素个数和待判断的查询个数。

第二行给出 $N$ 个互不相同的整数，表示入队序列。

接下来 $K$ 行，每行给出一个长度为 $N$ 的序列，表示一个候选出队序列。

## 输出描述

对于每个候选出队序列，输出一行：若该序列可以得到则输出 `yes`，否则输出 `no`。

## 样例1

### 输入

```

5 4
10 2 3 4 5
10 3 2 5 4
5 10 3 2 4
2 3 10 4 5
3 5 10 4 2

```

### 输出

```
yes
no
yes
yes
```

---

> 训练周次：WEEK4 栈、队列与单调结构（5.18-5.24）
> 原题来源：浙江大学 保研 012
> 原题路径：`schools/浙江大学/012-One Way In, Two Ways Out.md`
> 难度 / 范围：中等 / 数据结构,队列
