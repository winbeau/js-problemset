# A+B 加强版

#### 题目描述

读入两个小于 $100$ 的正整数 $A$ 和 $B$，计算 $A + B$。

需要注意的是： $A$ 和 $B$ 的每一位数字由对应的英文单词给出。

#### 输入格式

测试输入包含若干测试用例，每个测试用例占一行，格式为 `"A + B ="`，相邻两字符串有一个空格间隔。

当 $A$ 和 $B$ 同时为 $0$ 时输入结束，相应的结果不要输出。

#### 输出格式

对每个测试用例输出 $1$ 行，即 $A + B$ 的值。

#### 输入样例

```
one + two =
three four + five six =
zero seven + eight nine =
zero + zero =
```

#### 输出样例

```
3
90
96
```
