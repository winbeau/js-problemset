# 字符移动

#### 题目描述

输入一个字符串，将其中的数字字符移动到非数字字符之后，并保持数字字符和非数字字符输入时的顺序。

例如：输入字符串 $ab4f35gr\#a6$，输出为 $abfgr\#a4356$。

#### 输入格式

输入一行字符串，长度小于 $100$。

#### 输出格式

输出结果。

#### 输入样例
```
ab4f35gr#a6
```

#### 输出样例
```
abfgr#a4356
```
