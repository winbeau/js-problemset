# 球的半径和体积

#### 题目描述

输入球的中心点和球上某一点的坐标，计算球的半径和体积。

#### 输入格式

球的中心点和球上某一点的坐标，以如下形式输入：

`$x0$ $y0$ $z0$ $x1$ $y1$ $z1$`

#### 输出格式

对于每组输入，输出球的半径和体积，结果保留三位小数。

为避免精度问题，$PI$ 值请使用 $arccos(-1)$。

#### 输入样例

```
0 0 0 1 1 1
```

#### 输出样例

```
1.732 21.766
```
