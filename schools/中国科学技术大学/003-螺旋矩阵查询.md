# 螺旋矩阵查询

#### 题目描述

一个 $n$ 行 $m$ 列的螺旋矩阵可由如下方法生成：

从矩阵的左上角(第 $1$ 行第 $1$ 列)出发，初始时向右移动；如果前方是未曾经过的格子，则继续前进，否则右转；重复上述操作直至经过矩阵中所有格子。

根据经过顺序，在格子中依次填入 $1, 2, \ldots, n \times m$，便构成了一个螺旋矩阵。

以下是一个 $n=3$, $m=4$ 的螺旋矩阵：

```

1  2  3  4

10 11 12 5

9  8  7  6

```

现给定 $n$ 和 $m$ 构造一个螺旋矩阵，求这个矩阵第 $i$ 行第 $j$ 列的数字。

#### 输入格式

输入共一行四个正整数 $n$, $m$, $i$, $j$ (用空格隔开)。

#### 输出格式

输出共一行一个正整数，表示第 $i$ 行第 $j$ 列的数字。

#### 数据范围

- 对于 $50\%$ 数据，$1 \leq n, m \leq 100$。

- 对于 $100\%$ 数据，$1 \leq n, m \leq 10^5$，$1 \leq i \leq n$，$1 \leq j \leq m$。

#### 输入样例
```
3 4 2 3
```

#### 输出样例
```
12
```
