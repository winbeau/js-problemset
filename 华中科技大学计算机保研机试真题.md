[**完整题目及在线评测：https://pgcode.cn/**](https://pgcode.cn/)

## 梅森素数-华中科技大学

#### 题目描述

我们将满足 $ M = 2^k - 1 $ 的素数称为梅森素数，其中 $ k $ 也需要为某一素数。

比如 $ 3 = 2^2 - 1 $ 即满足这个条件，但是 $ 2^{11} - 1 = 2047 = 23 \times 89 $ 就不是梅森素数。

给定一个数 $ M $，顺序求出所有不大于 $ M $ 的梅森素数。

#### 输入格式

一个整数 $ M $。

#### 输出格式

每行输出一个梅森素数，格式为 `M(k) = M`，其中 $ k $ 是素数，$ M $ 是梅森素数。

#### 输入样例
```
97
```

#### 输出样例
```
M(2) = 3
M(3) = 7
M(5) = 31
```

## 字符串密码生成-华中科技大学

待添加

## 有趣的数-华中科技大学

待添加

## 回文字符串-华中科技大学

#### 题目描述

给出一个长度不超过 $1000$ 的字符串，判断它是不是回文（顺读，逆读均相同）。

#### 输入格式

输入包括一行字符串，其长度不超过 $1000$。

#### 输出格式

可能有多组测试数据，对于每组数据，如果是回文字符串则输出 $Yes!$，否则输出 $No!$。

#### 输入样例
```
hellolleh
hellowor1d
```

#### 输出样例
```
Yes!
No!
```

## 字符串处理-华中科技大学

待添加

## 字符串拼接与处理-华中科技大学

待添加

## 验证电话号码-华中科技大学

#### 题目描述

输入一串字符，并验证其是否为正确的电话号码。

长度必须为 $11$ 位。

第一位必须为 $1$ ，第二位为 $3$ 、 $5$ 、 $6$ 、 $8$ 中的一个，其余位必须为 $0$ - $9$ 中的一个数。

#### 输入格式

输入一串字符。

#### 输出格式

如果输入符合电话号码的规则，输出 $YES$ ；否则输出 $NO$ 。

#### 输入样例
```
13812345678
```

#### 输出样例
```
YES
```

## 孪生素数-华中科技大学

待添加

## 顺转数组-华中科技大学

待添加

## 最长子回文串-华中科技大学

#### 题目描述

输入一个字符串，求出其中最长的回文子串。

子串的含义是：在原串连续出现的字符串片段。

回文的含义是：正着看和倒着看是相同的，如 $abba$ 和 $abbebba$。

在判断时要求忽略所有的标点和空格，且忽略大小写，但输出时按原样输出（首尾不要输出多余的字符串）。如果存在多个，则输出第一个。

#### 输入格式

输入一个字符串，长度大于等于 $1$ 小于等于 $5000$。

#### 输出格式

输出字符串中最长的回文子串。

#### 输入样例
```
Madam,I'm Adam
```

#### 输出样例
```
Madam,I'm Adam
```

## 长整数中间两个字节循环左移-华中科技大学

待添加

## 无符号长整数字节交换-华中科技大学

待添加

## 公路建设问题-华中科技大学

#### 题目描述

给定一个带权值的无向图，图中的边和顶点都有编号。

第一问要求将所有的边按权值排序输出。

第二问要求求出图的最小生成树，输出具体路径和最小代价。

#### 输入格式

第一行包含两个整数 $n$ 和 $m$，分别表示顶点数和边数。

接下来 $m$ 行，每行包含三个整数 $u$、$v$ 和 $w$，表示顶点 $u$ 和顶点 $v$ 之间有一条权值为 $w$ 的边。

#### 输出格式

第一问输出 $m$ 行，每行包含三个整数 $u$、$v$ 和 $w$，表示按权值排序后的边。

第二问输出最小生成树的路径和最小代价，具体格式见样例。

#### 输入样例
```
4 5
1 2 2
1 3 4
2 3 3
2 4 1
3 4 5
```

#### 输出样例
```
2 4 1
1 2 2
2 3 3
1 3 4
3 4 5
最小生成树路径：
2 4 1
1 2 2
2 3 3
最小代价：6
```

## 矩阵转置-华中科技大学

待添加

## 切牌、洗牌-华中科技大学

待添加

## N阶楼梯上楼问题-华中科技大学

#### 题目描述

$N$ 阶楼梯上楼问题：一次可以走两阶或一阶，问有多少种上楼方式。

（要求采用非递归）

#### 输入格式

输入包括一个整数 $N$ ，$(1 \leq N < 90)$。

#### 输出格式

可能有多组测试数据，对于每组数据，输出当楼梯阶数是 $N$ 时的上楼方式个数。

#### 输入样例
```
4
```

#### 输出样例
```
5
```

## 题目描述-华中科技大学

待添加

## 二叉树遍历2-华中科技大学

待添加

## 打印日期-华中科技大学

#### 题目描述

给出年份 $y$ 和一年中的第 $n$ 天，算出第 $n$ 天是几月几号。

#### 输入格式

输入包括两个整数 $y$ $(1 \leq y \leq 3000)$, $n$ $(1 \leq n \leq 366)$。

#### 输出格式

可能有多组测试数据，对于每组数据，按 $yyyy-mm-dd$ 的格式将输入中对应的日期打印出来。

#### 输入样例
```
2000 3
2000 31
2000 40
2000 60
2000 61
2001 60
```

#### 输出样例
```
2000-01-03
2000-01-31
2000-02-09
2000-02-29
2000-03-01
2001-03-01
```

## 排序-华中科技大学

待添加

## 最长&最短文本-华中科技大学

待添加

## 统计单词-华中科技大学

#### 题目描述

编一个程序，读入用户输入的以 $.$ 结尾的一行文字，统计一共有多少个单词，并分别输出每个单词含有多少个字符。

（凡是以一个或多个空格隔开的部分就是一个单词）

#### 输入格式

输入包括 $1$ 行字符串，以 $.$ 结束，字符串中包含多个单词，单词之间以一个或多个空格隔开。

#### 输出格式

可能有多组测试数据，对于每组数据，输出字符串中每个单词包含的字母的个数。

#### 输入样例
```
hello how are you.
```

#### 输出样例
```
5 3 3 3
```

## 最小年龄的3个职工-华中科技大学

待添加

## 对称矩阵-华中科技大学

待添加

